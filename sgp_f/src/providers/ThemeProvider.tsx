import { ReactNode } from "react"

// Simple passthrough theme provider placeholder.
// Replace with your design system/theme logic when ready.
export function ThemeProvider({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
